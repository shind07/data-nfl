import os
import sys

# allow the pipeline to import modules rom parent dir (specifically db.py)
parent_dir = os.path.abspath(os.path.join(os.path.dirname(__file__), '..'))
if parent_dir not in sys.path:
    sys.path.insert(0, parent_dir)
